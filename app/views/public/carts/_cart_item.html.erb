<li class="list-none">
    <%= cart_item.item.name %>
    <%= cart_item.item.price %>
    <%= form_with(url: public_item_update_item_path, method: :post,local: true) do |f| %>
    <%= f.hidden_field :item_id, value: cart_item.item.id %>
    <%= f.number_field :quantity ,value: cart_item.quantity%>
    <%= f.submit "更新", class: "" %>
    <% end %>
    <%= button_to "削除", public_item_delete_item_path(item_id: cart_item.item.id), method: :delete,
                                       data: { confirm: "商品を削除しますか？" } %>
</li>